import {DashboardInfoItem} from './dashboard-info-item';

describe('DashboardInfoItem', () => {
    it('should create an instance', () => {
        expect(new DashboardInfoItem()).toBeTruthy();
    });
});
