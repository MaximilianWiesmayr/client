import {ErrorStateMatcher} from './form-error-state-matcher';

describe('ErrorStateMatcher', () => {
    it('should create an instance', () => {
        expect(new ErrorStateMatcher()).toBeTruthy();
    });
});
